# Instruct npm to ignore package-lock.json during installs (Vercel will install from package.json)
package-lock=false
# Ensure devDependencies are installed on build (so TypeScript and @types/* are present)
production=false
